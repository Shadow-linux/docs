---
title: 'predictLongRunning'
openapi: 'POST /v1beta/models/{model}:predictLongRunning'
---

使用 Veo 3 模型创建视频生成任务。支持文本到视频和基于参考图片的视频生成。

`model` 可使用完整的斜杠分段名称，例如 `google/veo-3.1-generate-preview`；`/`
属于 TokenOps 模型标识，不需要由客户端改写。
